#ifndef METHODGAUSS_H
#define METHODGAUSS_H

#include "../main.h"

bool	direct_traverse(vector<vector<VALUE_TYPE>> &slae, int size);
void	reverse_traverse(vector<vector<VALUE_TYPE>> &slae, int size);

#endif
